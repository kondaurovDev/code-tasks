/**
 * Задача 1: Поиск суммы элементов массива
 *
 * Напиши функцию, которая принимает массив чисел
 * и возвращает сумму его элементов.
 *
 * Пример:
 *   sumArray([1, 2, 3]) -> 6
 *   sumArray([10, -5, 5]) -> 10
 */

import { describe, it, expect } from "vitest";

// ===== ТВОЙ КОД ЗДЕСЬ =====

function sumArray(numbers: number[]): number {
  // TODO: напиши реализацию

  numbers.forEach(n => n + n)
  return 0;
}

// ===========================

describe("sumArray", () => {
  it("должна вернуть сумму чисел в массиве", () => {
    expect(sumArray([1, 2, 3])).toBe(6);
  });

  it("должна работать с отрицательными числами", () => {
    expect(sumArray([10, -5, 5])).toBe(10);
  });

  it("должна вернуть 0 для пустого массива", () => {
    expect(sumArray([])).toBe(0);
  });

  it("должна работать с одним элементом", () => {
    expect(sumArray([42])).toBe(42);
  });
});
