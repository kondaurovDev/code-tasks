/**
 * Задача 6: Линейный поиск
 *
 * Напиши функцию, которая выполняет линейный поиск элемента в массиве
 * и возвращает индекс этого элемента или -1, если элемент не найден.
 *
 * Пример:
 *   linearSearch([1, 5, 3, 9, 2], 3) -> 2
 *   linearSearch([1, 5, 3, 9, 2], 7) -> -1
 */

import { describe, it, expect } from "vitest";

// ===== ТВОЙ КОД ЗДЕСЬ =====

function linearSearch(arr: number[], target: number): number {
  // TODO: напиши реализацию
  return -1;
}

// ===========================

describe("linearSearch", () => {
  it("должна найти элемент и вернуть его индекс", () => {
    expect(linearSearch([1, 5, 3, 9, 2], 3)).toBe(2);
  });

  it("должна вернуть -1 если элемент не найден", () => {
    expect(linearSearch([1, 5, 3, 9, 2], 7)).toBe(-1);
  });

  it("должна найти первый элемент", () => {
    expect(linearSearch([1, 5, 3, 9, 2], 1)).toBe(0);
  });

  it("должна найти последний элемент", () => {
    expect(linearSearch([1, 5, 3, 9, 2], 2)).toBe(4);
  });

  it("должна вернуть -1 для пустого массива", () => {
    expect(linearSearch([], 5)).toBe(-1);
  });
});
