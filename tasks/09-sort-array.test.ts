/**
 * Задача 9: Сортировка массива
 *
 * Напиши функцию, которая сортирует массив чисел по возрастанию.
 *
 * Попробуй реализовать один из алгоритмов сортировки самостоятельно:
 * - Bubble Sort (сортировка пузырьком)
 * - Selection Sort (сортировка выбором)
 * - Insertion Sort (сортировка вставками)
 *
 * Пример:
 *   sortArray([3, 1, 4, 1, 5]) -> [1, 1, 3, 4, 5]
 *   sortArray([5, 4, 3, 2, 1]) -> [1, 2, 3, 4, 5]
 */

import { describe, it, expect } from "vitest";

// ===== ТВОЙ КОД ЗДЕСЬ =====

function sortArray(numbers: number[]): number[] {
  // TODO: напиши реализацию
  return [];
}

// ===========================

describe("sortArray", () => {
  it("должна отсортировать массив по возрастанию", () => {
    expect(sortArray([3, 1, 4, 1, 5])).toEqual([1, 1, 3, 4, 5]);
  });

  it("должна отсортировать обратно отсортированный массив", () => {
    expect(sortArray([5, 4, 3, 2, 1])).toEqual([1, 2, 3, 4, 5]);
  });

  it("должна вернуть пустой массив для пустого массива", () => {
    expect(sortArray([])).toEqual([]);
  });

  it("должна работать с одним элементом", () => {
    expect(sortArray([42])).toEqual([42]);
  });

  it("должна работать с отрицательными числами", () => {
    expect(sortArray([3, -1, 0, -5, 2])).toEqual([-5, -1, 0, 2, 3]);
  });
});
