/**
 * Задача 5: Поиск максимального и минимального элемента в массиве
 *
 * Напиши две функции:
 * - findMax: находит максимальный элемент в массиве
 * - findMin: находит минимальный элемент в массиве
 *
 * Пример:
 *   findMax([1, 5, 3, 9, 2]) -> 9
 *   findMin([1, 5, 3, 9, 2]) -> 1
 */

import { describe, it, expect } from "vitest";

// ===== ТВОЙ КОД ЗДЕСЬ =====

function findMax(numbers: number[]): number {
  // TODO: напиши реализацию
  return 0;
}

function findMin(numbers: number[]): number {
  // TODO: напиши реализацию
  return 0;
}

// ===========================

describe("findMax", () => {
  it("должна найти максимум в массиве", () => {
    expect(findMax([1, 5, 3, 9, 2])).toBe(9);
  });

  it("должна работать с отрицательными числами", () => {
    expect(findMax([-5, -1, -10])).toBe(-1);
  });

  it("должна работать с одним элементом", () => {
    expect(findMax([42])).toBe(42);
  });
});

describe("findMin", () => {
  it("должна найти минимум в массиве", () => {
    expect(findMin([1, 5, 3, 9, 2])).toBe(1);
  });

  it("должна работать с отрицательными числами", () => {
    expect(findMin([-5, -1, -10])).toBe(-10);
  });

  it("должна работать с одним элементом", () => {
    expect(findMin([42])).toBe(42);
  });
});
