/**
 * Задача 4: Фибоначчиева последовательность
 *
 * Напиши функцию, которая принимает число n и возвращает
 * n-ый элемент последовательности Фибоначчи.
 *
 * Последовательность Фибоначчи: 0, 1, 1, 2, 3, 5, 8, 13, 21, ...
 * Каждое число равно сумме двух предыдущих.
 *
 * Пример:
 *   fibonacci(0) -> 0
 *   fibonacci(1) -> 1
 *   fibonacci(6) -> 8
 */

import { describe, it, expect } from "vitest";

// ===== ТВОЙ КОД ЗДЕСЬ =====

function fibonacci(n: number): number {
  // TODO: напиши реализацию
  return 0;
}

// ===========================

describe("fibonacci", () => {
  it("должна вернуть 0 для n=0", () => {
    expect(fibonacci(0)).toBe(0);
  });

  it("должна вернуть 1 для n=1", () => {
    expect(fibonacci(1)).toBe(1);
  });

  it("должна вернуть 1 для n=2", () => {
    expect(fibonacci(2)).toBe(1);
  });

  it("должна вернуть 8 для n=6", () => {
    expect(fibonacci(6)).toBe(8);
  });

  it("должна вернуть 55 для n=10", () => {
    expect(fibonacci(10)).toBe(55);
  });
});
