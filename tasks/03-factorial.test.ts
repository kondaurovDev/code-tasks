/**
 * Задача 3: Факториал числа
 *
 * Напиши функцию, которая вычисляет факториал заданного числа.
 *
 * Факториал числа n (обозначается n!) - это произведение всех
 * натуральных чисел от 1 до n.
 *
 * Пример:
 *   factorial(5) -> 120  (потому что 5! = 5 * 4 * 3 * 2 * 1 = 120)
 *   factorial(0) -> 1    (по определению 0! = 1)
 *   factorial(1) -> 1
 */

import { describe, it, expect } from "vitest";

// ===== ТВОЙ КОД ЗДЕСЬ =====

function factorial(n: number): number {
  // TODO: напиши реализацию
  return 0;
}

// ===========================

describe("factorial", () => {
  it("должна вычислить факториал 5", () => {
    expect(factorial(5)).toBe(120);
  });

  it("должна вернуть 1 для 0", () => {
    expect(factorial(0)).toBe(1);
  });

  it("должна вернуть 1 для 1", () => {
    expect(factorial(1)).toBe(1);
  });

  it("должна вычислить факториал 3", () => {
    expect(factorial(3)).toBe(6);
  });

  it("должна вычислить факториал 10", () => {
    expect(factorial(10)).toBe(3628800);
  });
});
